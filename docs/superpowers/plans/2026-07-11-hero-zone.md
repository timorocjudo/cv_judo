# Hero Zone — Landing Page IpponId

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter une Hero Zone cinématique en haut de la landing page ipponid.com et une route OG dynamique pour les partages réseaux sociaux.

**Architecture:** Un nouveau composant client `HeroZone.tsx` est inséré avant `HeroSection` dans `app/page.tsx`. Il utilise Framer Motion pour des animations en cascade et Tailwind arbitrary values pour ses couleurs spécifiques (#1B3A6B, #D4A017). L'OG image est une route statique `/api/og/landing` calquée sur les routes `/api/og/profile` et `/api/og/result` déjà en place.

**Tech Stack:** Next.js 14 App Router, Framer Motion v12, `@vercel/og` (`ImageResponse`), Tailwind CSS, TypeScript.

## Global Constraints

- Pas de suite de tests configurée — la vérification est visuelle (navigateur, devtools responsive)
- `'use client'` obligatoire sur tout composant utilisant Framer Motion hooks
- Les polices OG sont lues depuis `public/fonts/Montserrat-Black.ttf` et `public/fonts/Inter-Bold.ttf` avec `fs.readFileSync`
- Couleurs hero : fond `#1B3A6B`, or CTA/wordmark `#D4A017`, cercles déco `#1E4A8A`
- La balise `<h1>` de la page appartient désormais au slogan dans `HeroZone` — le titre de `HeroSection` doit devenir `<h2>`
- Le bouton CTA pointe sur `/creer-mon-profil` (sans query param — l'utilisateur choisit son type sur la page suivante)
- `prefers-reduced-motion` : durées à 0 et translations à 0 via `useReducedMotion()` de Framer Motion
- `main` dans `page.tsx` a déjà `className="mt-20"` pour compenser la nav fixe — ne pas le changer

---

## File Map

| Fichier | Action |
|---|---|
| `components/landing/HeroZone.tsx` | Créer — composant client hero |
| `components/landing/HeroSection.tsx` | Modifier — `<h1>` → `<h2>` ligne 31 |
| `app/page.tsx` | Modifier — import + insertion JSX + metadata OG |
| `app/api/og/landing/route.tsx` | Créer — ImageResponse 1200×630 |

---

## Task 1 — Créer `components/landing/HeroZone.tsx`

**Files:**
- Create: `components/landing/HeroZone.tsx`

**Interfaces:**
- Produces: export default `HeroZone()` — React component, aucune prop
- Consumed by: Task 3 (`app/page.tsx`)

- [ ] **Step 1 : Créer le fichier avec le composant complet**

Créer `components/landing/HeroZone.tsx` avec ce contenu exact :

```tsx
'use client'

import Link from 'next/link'
import { motion, useReducedMotion } from 'framer-motion'

export default function HeroZone() {
  const prefersReduced = useReducedMotion() ?? false

  const dur = prefersReduced ? 0 : 0.4
  const yVal = prefersReduced ? 0 : 20

  return (
    <section className="relative overflow-hidden bg-[#1B3A6B] min-h-[60vh] md:min-h-[70vh] lg:min-h-[75vh] flex items-center justify-center px-margin-mobile md:px-margin-desktop py-20">
      {/* Decorative circles */}
      <div className="absolute -top-40 -left-40 w-[500px] h-[500px] rounded-full bg-[#1E4A8A] opacity-[0.15] pointer-events-none" />
      <div className="absolute -bottom-32 -right-32 w-[400px] h-[400px] rounded-full bg-[#1E4A8A] opacity-[0.15] pointer-events-none" />

      <div className="relative z-10 text-center max-w-4xl mx-auto w-full">
        {/* Wordmark */}
        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ duration: dur, delay: 0, ease: 'easeOut' }}
          className="font-montserrat text-[3rem] font-black tracking-tight"
        >
          <span className="text-white">Ippon</span>
          <span className="text-[#D4A017]">Id</span>
        </motion.div>

        {/* Decorative gold bar */}
        <motion.div
          initial={{ opacity: 0, scaleX: 0 }}
          animate={{ opacity: 1, scaleX: 1 }}
          transition={{ duration: dur, delay: prefersReduced ? 0 : 0.075, ease: 'easeOut' }}
          style={{ originX: 'center' }}
          className="w-20 h-0.5 bg-[#D4A017] mx-auto my-6"
        />

        {/* H1 */}
        <motion.h1
          initial={{ opacity: 0, y: yVal }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: dur, delay: prefersReduced ? 0 : 0.15, ease: 'easeOut' }}
          className="font-montserrat font-black text-white leading-tight mb-6"
          style={{ fontSize: 'clamp(2.5rem, 6vw, 4.5rem)', lineHeight: 1.1 }}
        >
          Ton palmarès mérite sa propre page.
        </motion.h1>

        {/* Subtitle */}
        <motion.p
          initial={{ opacity: 0, y: yVal }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: dur, delay: prefersReduced ? 0 : 0.3, ease: 'easeOut' }}
          className="font-inter text-white/70 text-lg md:text-xl max-w-2xl mx-auto mb-10"
        >
          Crée gratuitement ta page judoka avec ton palmarès, tes vidéos et ta galerie photo.
        </motion.p>

        {/* CTA */}
        <motion.div
          initial={{ opacity: 0, y: yVal }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: dur, delay: prefersReduced ? 0 : 0.45, ease: 'easeOut' }}
        >
          <Link
            href="/creer-mon-profil"
            className="inline-block bg-[#D4A017] text-[#1B3A6B] font-bold font-montserrat px-8 py-4 rounded-xl hover:shadow-lg hover:scale-[1.02] transition-all duration-150 w-full sm:w-auto text-center"
          >
            Créer mon profil gratuitement →
          </Link>
        </motion.div>
      </div>
    </section>
  )
}
```

- [ ] **Step 2 : Lancer le serveur de dev et vérifier visuellement**

```bash
npm run dev
```

Ouvrir `http://localhost:3000`. Vérifier :
- Fond bleu marine occupe bien ≥ 60vh
- Wordmark "Ippon" blanc / "Id" or (≈48px)
- Barre dorée fine apparaît sous le wordmark
- Slogan en grand, blanc, centré
- Sous-titre en blanc 70%
- Bouton CTA fond or, texte bleu marine

À ce stade, `HeroZone` n'est pas encore intégré dans `page.tsx` — ce fichier seul ne sera visible qu'après Task 3. Continuer directement à Task 2.

- [ ] **Step 3 : Type-check**

```bash
npx tsc --noEmit
```

Résultat attendu : aucune erreur TypeScript.

- [ ] **Step 4 : Commit**

```bash
git add components/landing/HeroZone.tsx
git commit -m "feat(landing): add HeroZone component with Framer Motion animations"
```

---

## Task 2 — Modifier `components/landing/HeroSection.tsx` (h1 → h2)

**Files:**
- Modify: `components/landing/HeroSection.tsx:31-33`

**Interfaces:**
- Consumes: rien de Task 1
- Produces: `HeroSection` inchangé fonctionnellement, mais le titre descend en `<h2>` pour laisser le `<h1>` à `HeroZone`

- [ ] **Step 1 : Remplacer `<h1>` par `<h2>` dans HeroSection**

Dans `components/landing/HeroSection.tsx`, ligne 31, remplacer :

```tsx
        <h1 className="font-montserrat text-headline-lg-mobile md:text-headline-lg font-black text-primary mb-6 leading-tight">
          Le CV en ligne des judokas
        </h1>
```

par :

```tsx
        <h2 className="font-montserrat text-headline-lg-mobile md:text-headline-lg font-black text-primary mb-6 leading-tight">
          Le CV en ligne des judokas
        </h2>
```

Aucun autre changement dans ce fichier.

- [ ] **Step 2 : Type-check**

```bash
npx tsc --noEmit
```

Résultat attendu : aucune erreur.

- [ ] **Step 3 : Commit**

```bash
git add components/landing/HeroSection.tsx
git commit -m "fix(landing): demote HeroSection title from h1 to h2"
```

---

## Task 3 — Modifier `app/page.tsx` (import + JSX + metadata OG)

**Files:**
- Modify: `app/page.tsx`

**Interfaces:**
- Consumes: `HeroZone` de Task 1 (export default de `components/landing/HeroZone`)
- Produces: landing page avec HeroZone visible en premier, OG image pointant vers `/api/og/landing`

- [ ] **Step 1 : Ajouter l'import de HeroZone**

Dans `app/page.tsx`, après la ligne 6 (`import HeroSection from ...`), ajouter :

```tsx
import HeroZone from '@/components/landing/HeroZone'
```

Le bloc d'imports ressemble à ceci après la modification :

```tsx
import LandingNav from '@/components/landing/LandingNav'
import LandingMobileNav from '@/components/landing/LandingMobileNav'
import HeroZone from '@/components/landing/HeroZone'
import HeroSection from '@/components/landing/HeroSection'
import MockupSection from '@/components/landing/MockupSection'
// …
```

- [ ] **Step 2 : Insérer `<HeroZone />` avant `<HeroSection />` dans le JSX**

Dans le `return` de `LandingPage`, remplacer :

```tsx
      <main className="mt-20">
        <HeroSection />
```

par :

```tsx
      <main className="mt-20">
        <HeroZone />
        <HeroSection />
```

- [ ] **Step 3 : Mettre à jour les métadonnées OG**

Dans l'objet `metadata` (lignes 14-33), remplacer les deux champs `images` :

```ts
// openGraph — avant :
    images: [{ url: '/images/cover.jpg', width: 1200, height: 630 }],

// openGraph — après :
    images: [{ url: '/api/og/landing', width: 1200, height: 630 }],
```

```ts
// twitter — avant :
    images: ['/images/cover.jpg'],

// twitter — après :
    images: ['/api/og/landing'],
```

- [ ] **Step 4 : Vérifier le rendu à trois tailles**

Le serveur de dev doit déjà tourner (`npm run dev`). Ouvrir `http://localhost:3000` et vérifier avec les DevTools (Ctrl+Shift+M ou F12 → responsive) :

**Mobile 375px :**
- HeroZone occupe ≥ 60vh
- Slogan sur 2 lignes max
- Bouton CTA pleine largeur (`w-full`)
- Cercles déco non visibles au centre

**Tablette 768px :**
- HeroZone occupe ≥ 70vh
- Slogan sur 1-2 lignes, centré
- Bouton auto-width, centré

**Desktop 1280px :**
- HeroZone occupe ≥ 75vh
- Slogan sur une ligne
- Sous-titre sur une ligne
- Bouton auto-width, centré

- [ ] **Step 5 : Type-check**

```bash
npx tsc --noEmit
```

Résultat attendu : aucune erreur.

- [ ] **Step 6 : Commit**

```bash
git add app/page.tsx
git commit -m "feat(landing): integrate HeroZone and update OG metadata"
```

---

## Task 4 — Créer `app/api/og/landing/route.tsx`

**Files:**
- Create: `app/api/og/landing/route.tsx`

**Interfaces:**
- Consumes: `public/fonts/Montserrat-Black.ttf`, `public/fonts/Inter-Bold.ttf` (déjà présents)
- Produces: `GET /api/og/landing` → PNG 1200×630 pour les previews réseaux sociaux

- [ ] **Step 1 : Créer la route OG**

Créer `app/api/og/landing/route.tsx` avec ce contenu :

```tsx
import { ImageResponse } from '@vercel/og'
import fs from 'fs'
import path from 'path'

const montserratBlack = fs.readFileSync(
  path.join(process.cwd(), 'public/fonts/Montserrat-Black.ttf')
)
const interBold = fs.readFileSync(
  path.join(process.cwd(), 'public/fonts/Inter-Bold.ttf')
)

export async function GET() {
  return new ImageResponse(
    (
      <div
        style={{
          display: 'flex',
          width: '1200px',
          height: '630px',
          backgroundColor: '#1B3A6B',
          position: 'relative',
          overflow: 'hidden',
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        {/* Cercle déco top-left */}
        <div
          style={{
            position: 'absolute',
            top: '-200px',
            left: '-200px',
            width: '600px',
            height: '600px',
            borderRadius: '300px',
            backgroundColor: '#1E4A8A',
            opacity: 0.15,
          }}
        />
        {/* Cercle déco bottom-right */}
        <div
          style={{
            position: 'absolute',
            bottom: '-150px',
            right: '-150px',
            width: '500px',
            height: '500px',
            borderRadius: '250px',
            backgroundColor: '#1E4A8A',
            opacity: 0.15,
          }}
        />

        {/* Contenu centré */}
        <div
          style={{
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            textAlign: 'center',
          }}
        >
          {/* Wordmark */}
          <div
            style={{
              display: 'flex',
              fontFamily: 'Montserrat',
              fontSize: '72px',
              fontWeight: 900,
              letterSpacing: '-0.02em',
            }}
          >
            <span style={{ color: 'white' }}>Ippon</span>
            <span style={{ color: '#D4A017' }}>Id</span>
          </div>

          {/* Barre or */}
          <div
            style={{
              width: '80px',
              height: '4px',
              backgroundColor: '#D4A017',
              margin: '24px 0',
            }}
          />

          {/* Slogan */}
          <div
            style={{
              fontFamily: 'Montserrat',
              fontSize: '56px',
              fontWeight: 900,
              color: 'white',
              lineHeight: 1.1,
              textAlign: 'center',
              maxWidth: '900px',
            }}
          >
            Ton palmarès mérite sa propre page.
          </div>

          {/* Sous-titre */}
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: '22px',
              fontWeight: 700,
              color: 'rgba(255,255,255,0.6)',
              marginTop: '24px',
              textAlign: 'center',
              maxWidth: '700px',
            }}
          >
            Crée gratuitement ta page judoka avec ton palmarès, tes vidéos et ta galerie photo.
          </div>
        </div>

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: '24px',
            right: '64px',
            fontFamily: 'Inter',
            fontSize: '13px',
            fontWeight: 700,
            color: 'rgba(255,255,255,0.3)',
            textTransform: 'uppercase',
            letterSpacing: '0.12em',
          }}
        >
          ipponid.com
        </div>
      </div>
    ),
    {
      width: 1200,
      height: 630,
      fonts: [
        {
          name: 'Montserrat',
          data: montserratBlack as unknown as ArrayBuffer,
          weight: 900,
          style: 'normal',
        },
        {
          name: 'Inter',
          data: interBold as unknown as ArrayBuffer,
          weight: 700,
          style: 'normal',
        },
      ],
      headers: { 'Cache-Control': 'public, max-age=604800, immutable' },
    }
  )
}
```

- [ ] **Step 2 : Vérifier l'image OG en dev**

Avec le serveur de dev actif, ouvrir dans le navigateur :

```
http://localhost:3000/api/og/landing
```

Résultat attendu : image PNG 1200×630 avec fond bleu marine, wordmark IpponId, slogan centré, watermark ipponid.com en bas à droite. Si le navigateur affiche une erreur 500, vérifier que `public/fonts/Montserrat-Black.ttf` et `public/fonts/Inter-Bold.ttf` existent bien.

- [ ] **Step 3 : Type-check**

```bash
npx tsc --noEmit
```

Résultat attendu : aucune erreur.

- [ ] **Step 4 : Commit**

```bash
git add app/api/og/landing/route.tsx
git commit -m "feat(og): add dynamic OG image route for landing page"
```

---

## Task 5 — Vérification finale

**Files:** aucun fichier à modifier — vérification uniquement.

- [ ] **Step 1 : Lint**

```bash
npm run lint
```

Résultat attendu : aucune erreur ni warning.

- [ ] **Step 2 : Type-check globale**

```bash
npx tsc --noEmit
```

Résultat attendu : aucune erreur.

- [ ] **Step 3 : Vérifier prefers-reduced-motion**

Dans Chrome DevTools → Rendering → Emulate CSS media feature `prefers-reduced-motion: reduce`. Recharger `http://localhost:3000`. Vérifier que le contenu de HeroZone apparaît immédiatement sans transition ni décalage.

- [ ] **Step 4 : Vérifier la cascade d'animation (motion activée)**

Désactiver l'émulation reduced-motion. Recharger la page. Observer l'ordre d'apparition :
1. Wordmark — apparaît en premier (fade, 0ms)
2. Barre or — apparaît en deuxième (fade + scale, 75ms)
3. Slogan h1 — 150ms
4. Sous-titre — 300ms
5. Bouton CTA — 450ms

- [ ] **Step 5 : Vérifier le hover CTA**

Survoler le bouton "Créer mon profil gratuitement →". Observer : légère ombre + scale 1.02 en 150ms.

- [ ] **Step 6 : URL de l'OG image à tester**

L'image est disponible directement sur :
```
http://localhost:3000/api/og/landing       ← dev local
https://ipponid.com/api/og/landing         ← production
```

Pour valider après déploiement :
- **Twitter Cards Validator :** https://cards-dev.twitter.com/validator
- **Facebook Sharing Debugger :** https://developers.facebook.com/tools/debug/

- [ ] **Step 7 : Commit final (si des ajustements ont été faits)**

```bash
git add -p
git commit -m "fix(landing): hero zone visual adjustments"
```
